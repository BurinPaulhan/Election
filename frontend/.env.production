# Frontend — variables de PRODUCTION (chargées par `vite build`)
# Ce fichier est volontairement committé : l'URL de l'API n'est pas un secret.
# À mettre à jour si l'URL réelle du backend Render change.

VITE_API_URL=https://aema-campaign-api.onrender.com